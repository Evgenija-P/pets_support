export const selectCurrentObj = state => state.current;
export const selectcurrent = state => state.current.currentNotices;

export const selectIsLoading = state => state.current.isLoading;
export const selectError = state => state.current.error;
